apiVersion: v1
kind: Service

metadata:
    name: cloudcart-db-service

spec:
  selector:
    app: cloudcart-db
   
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306

  type: ClusterIP
